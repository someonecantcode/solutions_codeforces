#include <bits/stdc++.h>
using namespace std;
#define ll long long;

int main() {
	string s;
	cin >> s;
	int count = 0;

	for(char c : s){
		if(c == 'a'){
			count++;
		}
	}
	cout << min(s.size(), 2*count -1);
}
